import React from 'react';

const Course = (props) => {
    const {name,img,description,price,rating} = props.course;
    return (
        <div>
            
        </div>
    );
};

export default Course;